import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Solar-System-sim',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
